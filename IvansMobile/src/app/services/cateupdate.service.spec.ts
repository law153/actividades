/*import { TestBed } from '@angular/core/testing';

import { CateupdateService } from './cateupdate.service';

describe('CateupdateService', () => {
  let service: CateupdateService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(CateupdateService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});*/
